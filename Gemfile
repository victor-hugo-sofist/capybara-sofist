# frozen_string_literal: true

source 'https://rubygems.org'

gem 'bundler', '< 3.0'
gemspec

gem 'xpath', github: 'teamcapybara/xpath'

# gem 'rack-test', github: 'rack/rack-test'

group :doc do
  gem 'redcarpet', platforms: :mri
end

gem 'pry' #Pry - permite inserir o binding.pry para se realizar a atividade de debug do código.

gem 'httparty'

gem 'json'

gem 'jsonpath'

gem 'logger'
